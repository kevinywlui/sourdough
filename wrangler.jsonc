// Cloudflare deployment config: a static-assets-only Worker, no build step.
// Only public/ is deployed — README, tests, and config stay out of the site.
{
  "name": "sourdough",
  "compatibility_date": "2026-09-06",
  "assets": {
    "directory": "./public"
  }
}
